# build output
dist/
.output/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

pnpm-lock.yaml

.astro
# Claude Code local settings (machine-specific)
.claude/settings.local.json

# CI 워크플로: 현재 PAT에 workflow 스코프 없어 push 불가하여 커밋 보류(파일은 로컬 유지)
.github/workflows/
